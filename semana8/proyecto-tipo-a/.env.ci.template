# .env.ci.template - plantilla para CI (NO subir valores sensibles)
# Reemplaza DOMAIN_SUFFIX por el sufijo real en secrets de GitHub o en tu .env.ci local

# Configuración específica para dominios tipo A (Clínica Dental SmileCenter)
API_HOST=api-tipo-a-${DOMAIN_SUFFIX}
DATABASE_URL=postgresql://user:pass@db-tipo-a-${DOMAIN_SUFFIX}/app
REDIS_URL=redis://cache-tipo-a-${DOMAIN_SUFFIX}:6379
ENVIRONMENT=ci
LOG_LEVEL=INFO
